# Hero imagery

Drop the Firefly-generated hero background as `hero-bg.jpg` (or `.png`) in this folder.

Expected dimensions: 16:9 or 21:9 horizontal, ideally 2400px+ wide. The image is rendered behind the hero text at low opacity with a parchment-color fade so the headline stays readable.

When the file lands, the Hero component will start using it automatically.
